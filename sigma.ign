enum CursorType {
	Default = nil,
	Hand,
	Pointer,
	Grab,
}

let cursor = CursorType::Hand

match cursor {
	CursorType::Pointer => println("its a pointer"),
	CursorType::Hand => println("its a hand")
}
