class Thing {
	constructor(self, x) {
		self.x = x
	}

	fn __add__(self, other) {
		if typeof(other) == "number" {
			new Thing(self.x + other)
		} else {
			new Thing(self.x + other.x)
		}
	}

	fn __radd__(self, other) {
		return Thing::__add__(self, other)
	}
}

println(10 + new Thing(5))
